# Contribution Guide

## Setup
1. `npm install`
2. `npm run lint`
3. `npm run typecheck`
4. `npm run test`

## Standards
- TypeScript strict mode across all workspaces.
- Keep modules focused by domain boundaries.
- Add tests for new contracts and skill logic.

## Pull requests
- Keep changes scoped to one capability.
- Include impact summary and validation notes.
